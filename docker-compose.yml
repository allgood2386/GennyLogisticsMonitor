version: '3.8'
services:
  gennylogisticsmonitor:
    build: .
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_RACE_MONITOR_API_KEY=your_api_key_here
    # Optionally, if you use a .env file, you can add:
    # env_file:
    #   - .env
    restart: unless-stopped
